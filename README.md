# Telegram ConsultOS Bot

Этот проект представляет собой многофункционального Telegram-чат-бота, разработанного для автоматизации процесса записи на консультации. Бот включает в себя полную воронку взаимодействия с пользователем: от приветствия и оплаты до ветвящегося опросника и бронирования временного слота.

## ✨ Возможности

-   **Интеграция с платежной системой:** Прием платежей через **ЮKassa** с использованием прямого API.
-   **Ветвящийся опросник:** Динамическая анкета с разными типами вопросов (одиночный выбор, текстовый ввод).
-   **Система бронирования:** Пользователи могут выбирать удобную дату и время из доступных слотов.
-   **Автоматическое управление слотами:** Забронированные слоты автоматически становятся недоступными.
-   **Админ-панель:** Удобный интерфейс для администраторов для управления временными слотами и просмотра записей.
-   **Уведомления:** Автоматическая отправка уведомлений администраторам о новых оплатах и записях.
-   **FSM (Finite State Machine):** Управление состоянием диалога для каждого пользователя.
-   **База данных:** Сохранение информации о пользователях, платежах, ответах на анкеты и бронированиях.

## 🛠️ Технологический стек

-   **Python 3.11+**
-   **aiogram 3.x:** Асинхронный фреймворк для создания Telegram-ботов.
-   **PostgreSQL:** Реляционная база данных для хранения данных.
-   **SQLAlchemy / asyncpg:** ORM и асинхронный драйвер для работы с PostgreSQL.
-   **Alembic:** Инструмент для миграций базы данных.
-   **Docker & Docker Compose:** Контейнеризация для удобного развертывания и запуска.
-   **Pydantic:** Валидация данных и управление настройками.

## 🚀 Установка и запуск

Проект поддерживает два режима запуска: **Long Polling** для локальной разработки и **Webhook** для "боевого" сервера. Бот автоматически выбирает режим в зависимости от того, заполнена ли переменная `WEBHOOK_HOST` в `.env` файле.

### 1. Начальная настройка

1.  **Клонировать репозиторий:**
    ```bash
    git clone https://github.com/kiore73/telegram-ConsultOS-bot.git
    cd telegram-ConsultOS-bot
    ```

2.  **Создать `.env` файл:**
    Скопируйте файл-пример:
    ```bash
    cp .env.example .env
    ```
    Откройте файл `.env` и обязательно заполните `BOT_TOKEN`, `ADMIN_IDS`, `SERVICE_PRICE` и настройки ЮKassa.

### 2. Режим для локальной разработки (Long Polling)

Это самый простой способ запустить бота на вашем компьютере для тестов.

1.  **Настроить `.env`:**
    -   Убедитесь, что переменная `WEBHOOK_HOST` **оставлена пустой**:
      ```
      WEBHOOK_HOST=
      ```

2.  **Запустить проект:**
    ```bash
    docker compose up -d
    ```
    Бот запустится в режиме опроса серверов Telegram.

### 3. Режим для "боевого" сервера (Webhook)

Этот режим рекомендуется для серверов, так как он более производительный и подходит для публичного размещения.

**Предварительные требования (для Ubuntu 24.04):**

*   **Docker и Docker Compose:**
    ```bash
    # Установка Docker
    sudo apt update
    sudo apt install ca-certificates curl gnupg
    sudo install -m 0755 -d /etc/apt/keyrings
    curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
    sudo chmod a+r /etc/apt/keyrings/docker.gpg
    echo \
      "deb [arch="$(dpkg --print-architecture)" signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
      "$(. /etc/os-release && echo "$VERSION_CODENAME")" stable" | \
      sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
    sudo apt update
    sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

    # Добавление пользователя в группу docker (чтобы не использовать sudo с docker)
    sudo usermod -aG docker $USER
    # Перезагрузите сессию или выйдите/войдите, чтобы изменения вступили в силу
    # newgrp docker
    ```

*   **Nginx:**
    ```bash
    sudo apt install nginx
    ```

*   **Certbot (для SSL-сертификатов):**
    ```bash
    sudo apt install certbot python3-certbot-nginx
    ```

**Пошаговая инструкция:**

1.  **Настроить `.env` на сервере:**
    -   Заполните все переменные, как в локальной настройке.
    -   **Обязательно** укажите ваш домен в переменной `WEBHOOK_HOST`:
      ```
      WEBHOOK_HOST=https://your-domain.com
      ```

2.  **Настроить Nginx в качестве обратного прокси:**
    Nginx будет принимать HTTPS-трафик от Telegram и перенаправлять его на нашего бота, работающего в Docker.

    -   Создайте файл конфигурации для вашего сайта (замените `your-domain.com` на ваш домен):
      ```bash
      sudo nano /etc/nginx/sites-available/your-domain.com
      ```
    -   Вставьте в него следующую конфигурацию, заменив `your-domain.com` на ваш домен:
      ```nginx
      server {
          listen 80;
          server_name your-domain.com;
          # Редирект с HTTP на HTTPS
          return 301 https://$server_name$request_uri;
      }

      server {
          listen 443 ssl http2;
          server_name your-domain.com;

          # Пути к вашим SSL-сертификатам будут указаны Certbot
          # ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
          # ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

          # Включаем Certbot для автоматической настройки SSL
          include /etc/letsencrypt/options-ssl-nginx.conf;
          ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

          location / {
              # Перенаправляем все запросы к нашему боту
              proxy_pass http://127.00.1:8080;
              proxy_set_header Host $host;
              proxy_set_header X-Real-IP $remote_addr;
              proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          }
      }
      ```
    -   Сохраните файл и создайте символическую ссылку:
      ```bash
      sudo ln -s /etc/nginx/sites-available/your-domain.com /etc/nginx/sites-enabled/
      ```
    -   Проверьте синтаксис Nginx и перезагрузите его:
      ```bash
      sudo nginx -t
      sudo systemctl reload nginx
      ```

3.  **Получить SSL-сертификат с помощью Certbot:**
    ```bash
    sudo certbot --nginx -d your-domain.com
    ```
    Следуйте инструкциям Certbot. Он автоматически обновит конфигурацию Nginx с путями к сертификатам.

4.  **Настроить Firewall (UFW):**
    Убедитесь, что ваш файрвол разрешает трафик на порты 80 (HTTP) и 443 (HTTPS):
    ```bash
    sudo ufw allow 'Nginx Full'
    sudo ufw enable # Если UFW не был включен
    ```

5.  **Запустить проект:**
    Находясь в директории проекта на сервере, выполните:
    ```bash
    docker compose up -d
    ```
    Бот автоматически запустится в режиме вебхука, так как `WEBHOOK_HOST` указан.

## ⚙️ Использование

### Пользовательский сценарий

1.  Найдите вашего бота в Telegram и отправьте команду `/start`.
2.  Бот предложит описание услуги и кнопку для перехода к оплате.
3.  Нажмите на кнопку, и бот сгенерирует ссылку на оплату через ЮKassa. Перейдите по ней и совершите тестовый платеж.
4.  Вернитесь в бот и нажмите кнопку "Я оплатил".
5.  После подтверждения оплаты бот предложит пройти опросник.
6.  Ответьте на вопросы анкеты.
7.  После анкеты выберите удобную дату и время для консультации.
8.  Подтвердите бронирование. Готово!

### Админский сценарий

1.  Если ваш Telegram ID указан в `ADMIN_IDS`, отправьте боту команду `/admin`.
2.  Вам откроется админ-панель с возможностями:
    -   **➕ Добавить слот:** Позволяет создать новую дату и время, доступные для бронирования.
    -   **👀 Список записей:** Показывает все подтвержденные бронирования.
    -   **❌ Отменить запись:** (В разработке)
3.  Вы также будете получать уведомления о каждой новой оплате и каждой новой записи на консультацию.

---

